<nav class="navbar navbar-default" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand fa fa-cloud" href="/"> Everest App</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">

      <ul class="nav navbar-nav navbar-right">
        <li class="dropdown">
          <% if current_user %>
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Welcome <%= current_user.name %> <%= image_tag avatar, :class => "navimage" %><b class="caret"></b></a>
          <ul class="dropdown-menu">


          <li><%= link_to " Goals", goals_path, class: "fa fa-star"  %></li>
          <li><%= link_to " My profile", user_path(current_user.id), class: "fa fa-user" %></li>

        
            <li class="divider"></li>
                      <li><%= link_to " Sign Out", "/logout", class: "fa fa-sign-out" %></li>
            <% end %>
          </ul>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>

  <!-- Flash Notice Alert -->
  <% if flash[:notice] %>
      <div class="alert alert-success text-center vcenter">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <strong><%= flash[:notice] %><strong></div>
  <% end %>

  <!-- Flash Error Alert -->
  <% if flash[:error] %>
      <div class="alert alert-warning text-center vcenter">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        <strong><%= flash[:error] %><strong></div>
  <% end %>


